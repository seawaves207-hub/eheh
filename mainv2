local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Dumb Destroyer",
   Icon = 1399354816, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Meow Meow nuzzles you~",
   LoadingSubtitle = "by SeaWaves",
   ShowText = "Rayfield", -- for mobile users to unhide Rayfield, change if you'd like
   Theme = "Bloom", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from emitting warnings when the script has a version mismatch with the interface.

   -- ScriptID = "sid_xxxxxxxxxxxx", -- Your Script ID from developer.sirius.menu — enables analytics, managed keys, and script hosting

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Nya~"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include Discord.gg/. E.g. Discord.gg/ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the Discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "Meows nier blocker",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique, as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"KwvRc5jbkh7rKF"} -- List of keys that the system will accept, can be RAW file links (pastebin, github, etc.) or simple strings ("hello", "key22")
   }
})

local MainTab = Window:CreateTab("❤️Visuals", 0) -- Title, Image
local MainSection = MainTab:CreateSection("ESP")

local ESPControls = loadstring(game:HttpGet("https://raw.githubusercontent.com/seawaves207-hub/eheh/refs/heads/main/test2333"))()

MainTab:CreateToggle({
    Name = "Box ESP", CurrentValue = false, Flag = "BoxToggle",
    Callback = function(Value) ESPControls.SetBoxEnabled(Value) end,
})

MainTab:CreateToggle({
    Name = "Health ESP", CurrentValue = false, Flag = "HealthToggle",
    Callback = function(Value) ESPControls.SetHealthEnabled(Value) end,
})

MainTab:CreateToggle({
    Name = "Name ESP", CurrentValue = false, Flag = "NameToggle",
    Callback = function(Value) ESPControls.SetNameEnabled(Value) end,
})

local NameSizeSlider = MainTab:CreateSlider({
   Name = "Name Size",
   Range = {8, 50}, 
   Increment = 1,
   Suffix = "px",
   CurrentValue = 14, 
   Flag = "NameSizeSlider",
   Callback = function(Value)
       ESPControls.SetNameSize(Value)
   end,
})

local HealthSizeSlider = MainTab:CreateSlider({
   Name = "Health Bar Size",
   Range = {1, 5},
   Increment = 1,
   Suffix = "px",
   CurrentValue = 1,
   Flag = "HealthSizeSlider",
   Callback = function(Value)
       ESPControls.SetHealthSize(Value)
   end,
})

local MainSection = MainTab:CreateSection("Item ESP")

local ItemESPControls = loadstring(game:HttpGet("https://raw.githubusercontent.com/seawaves207-hub/eheh/refs/heads/main/itemESP23"))()

MainTab:CreateToggle({
    Name = "Item Box", CurrentValue = false, Flag = "ItemBoxToggle",
    Callback = function(Value) ItemESPControls.SetBoxEnabled(Value) end,
})

MainTab:CreateToggle({
    Name = "Item Name", CurrentValue = false, Flag = "ItemNameToggle",
    Callback = function(Value) ItemESPControls.SetNameEnabled(Value) end,
})

local SecondTab = Window:CreateTab("test", 0) -- Title, Image
